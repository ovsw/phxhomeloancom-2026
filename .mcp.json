{
  "mcpServers": {
    "shadcn": {
      "command": "sh",
      "args": [
        "-c",
        "cd \"${CLAUDE_PROJECT_DIR:-.}/frontend\" && exec node_modules/.bin/shadcn mcp"
      ]
    },
    "sanity": {
      "command": "sh",
      "args": [
        "-c",
        "cd \"${CLAUDE_PROJECT_DIR:-.}/studio\" && set -a && . ./.env && set +a && exec npx -y mcp-remote https://mcp.sanity.io --transport http-only --header \"Authorization: Bearer ${SANITY_AUTH_TOKEN}\""
      ]
    }
  }
}
